[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "note2slides"
version = "0.1.0"
description = "note 記事(Markdown)から eラーニング用のプレゼンテーション(.pptx)とスライド画像を生成する"
requires-python = ">=3.10"
dependencies = [
    "python-pptx>=1.0",
    "markdown-it-py>=3.0",
    "pypdfium2>=4.0",  # PDF をスライド画像に描画する
    "pillow>=10.0",    # 描画結果を PNG / JPEG として書き出す
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
note2slides = "note2slides.cli:main"
note2slides-images = "note2slides.images_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
# 一時ファイルはリポジトリ内に置き、環境によらず同じ場所を使う
addopts = "--basetemp=build/pytest-tmp"
markers = ["slow: 外部ツール(LibreOffice)を起動する時間のかかるテスト"]
