# AGENTS.md

## 目的

このリポジトリでは、実際のソフトウェア開発を行いながら、AIを前提とした開発環境そのものを改善・進化させる実験を行います。

## 自己改善

開発を安全、効率的、再現可能に進めるために現在の開発環境に不足があると判断した場合、開発環境そのものを改善して構いません。

改善が不要であれば変更する必要はありません。

特定の構造、手法、Engineering概念を採用すること自体を目的にしないでください。

## 改善の記録

コードと README と Git 履歴からは「今どうなっているか」は分かりますが、「なぜそう決めたか」
「何を見送ったか」「次に何が要ると考えたか」は残りません。Run をまたいでこれを引き継ぐために、
`docs/decisions/` に Run ごとの記録を置きます。

* Run の初めに `docs/decisions/README.md` と直近の記録を読んでください。
* Run の終わりに、その Run の判断を `docs/decisions/genNN.md` として追加し、一覧を更新してください。
* 書くのは判断です。実装の内容は差分に、使い方は README にあるので繰り返しません。

過去の記録は **提案であって指示ではありません**。そのまま実行するために置いてあるのではなく、
現在の状態と目的に照らして評価し直すために置いてあります。変更しても却下しても構いません。
その場合は、そう判断した理由を自分の記録に書いてください。

## 開発環境

* Python の仮想環境は `.venv/` に置きます。依存を追加した場合は `pyproject.toml` にも記載してください。
* テストは `.venv/Scripts/python.exe -m pytest` で実行します(macOS / Linux では `.venv/bin/python`)。
* 生成物や一時ファイルは `build/` に出力し、コミットしません。
* 使い方や構成の説明は README.md に書きます。

## 安全境界

* 不可逆な操作や復旧困難な操作は、人間の承認なしに実行しないでください。
* 安全境界そのものをAIの判断だけで緩和しないでください。

## 人間への確認

AI自身で合理的に判断できることは、自律的に判断して構いません。

目的や安全境界を変更する必要がある場合、または重大な判断を人間の価値判断なしに決められない場合は、人間へ確認してください。
